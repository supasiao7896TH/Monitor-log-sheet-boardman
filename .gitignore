.claude/settings.local.json
node_modules
dist
.migration-tmp
bridge/*.log

# V29.78 FEAT: archived source log sheets (bridge auto-archive lands these at repo root) — data files,
# not source code. Root-anchored so it never ignores a future nested test fixture.
/*.xlsm
/*.xls

# V29.80 FEAT: auto-archive now files these one level down, into a monthly subfolder (e.g. "Aug 26")
# instead of directly at repo root — same data-files-not-source-code reasoning as above.
/*/*.xlsm
/*/*.xls

# V29.85 FEAT: shared-db sync snapshot ($SharedDbPath in bridge/excel-bridge.ps1) — real plant
# operational data (Tags/Records/MasterTags/UserCountermeasures), not source code, lands inside the
# repo folder at runtime. Same reasoning as the archived .xls/.xlsm rule above.
/shared-data/
